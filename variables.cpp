#include<bits/stdc++.h>
#include "class.h"
using namespace std; 	




/*	for(int i=0;i<n;i++)
	{
		if(s.boardState[i][0].size()==0)
			continue ;
		numStone = s.boardState[i][0][s.boardState[i][0].size()-1];	
		if(numStone==p || numStone==q)
		{
			onefirstrow=true;
		}	
		else if(numStone==r || numStone==s)
		{
			secondfirstrow=true;
		}
	}
	if(onefirstrow==true||secondfirstrow==true)
	for(int i=0;i<n && ;i++)
	{
		if(s.boardState[i][0].size()==0)
			continue ;
		numStone = s.boardState[i][0][s.boardState[i][0].size()-1];	
		if((numStone==p || numStone==q) && onefirstrow==true)
		{
			one=true;
		}	
		else if((numStone==r || numStone==s) && secondfirstrow==true)
		{
			two=true;
		}
	}


	for(int j=0;j<n;j++)
	{
		if(s.boardState[0][j].size()==0)
			continue ;
		numStone = s.boardState[0][j][s.boardState[0][j].size()-1];	
		if(numStone==p || numStone==q)
		{
			onefirstrow=true;
		}	
		else if(numStone==r || numStone==s)
		{
			secondfirstrow=true;
		}
	}
	if(onefirstrow==true||secondfirstrow==true)
	for(int i=0;i<n && ;i++)
	{
		if(s.boardState[i][0].size()==0)
			continue ;
		numStone = s.boardState[i][0][s.boardState[i][0].size()-1];	
		if((numStone==p || numStone==q) && onefirstrow==true)
		{
			one=true;
		}	
		else if((numStone==r || numStone==s) && secondfirstrow==true)
		{
			two=true;
		}
	}
*/